# Phase 4: Full NEAT-style evolution of topology.
# - Start with no hidden nodes (num_hidden = 0)
# - Node add/delete ON
# - Connection add/delete ON
# - Canonical "minimal start, complexify over time" behavior

# NEAT configuration for a LunarLander controller using NEAT-Python.
# Observation space: 8 floats  -> num_inputs  = 8
# Action space: Discrete(4)    -> num_outputs = 4

[NEAT]
fitness_criterion     = max
fitness_threshold     = 250.0
pop_size              = 50
reset_on_extinction   = False
no_fitness_termination= False

[DefaultGenome]
# network parameters
num_hidden              = 6
num_inputs              = 8
num_outputs             = 4
feed_forward            = True

# connection weight options
weight_init_mean        = 0.0
weight_init_stdev       = 1.0
weight_max_value        = 30
weight_min_value        = -30
weight_mutate_power     = 0.25
weight_mutate_rate      = 0.2
weight_replace_rate     = 0.2

# node aggregation options
aggregation_default     = sum
aggregation_mutate_rate = 0.0
aggregation_options     = sum

# node bias options
bias_init_mean          = 0.0
bias_init_stdev         = 1.0
bias_max_value          = 100.0
bias_min_value          = -100.0
bias_mutate_power       = 0.25
bias_mutate_rate        = 0.2
bias_replace_rate       = 0.1

# node activation options
activation_default      = clamped
activation_mutate_rate  = 0.0
activation_options      = clamped tanh relu sigmoid

# genome compatibility options
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient   = 0.5

# connection add/remove rates
conn_add_prob           = 0.15
conn_delete_prob        = 0.15

# connection enable options
enabled_default         = True
enabled_mutate_rate     = 0.01

# node add/remove rates
node_add_prob           = 0.15
node_delete_prob        = 0.15

# Initial connectivity pattern:
#   - "full_direct" = fully connect inputs directly to outputs (no hidden).
initial_connection      = full_direct

# node response options
response_init_mean      = 1.0
response_init_stdev     = 0.0
response_max_value      = 30.0
response_min_value      = -30.0
response_mutate_power   = 0.0
response_mutate_rate    = 0.0
response_replace_rate   = 0.0

# other parameters (don't change)
single_structural_mutation = false
structural_mutation_surer = default
bias_init_type = gaussian
response_init_type = gaussian
weight_init_type = gaussian
enabled_rate_to_true_add = 0.0
enabled_rate_to_false_add = 0.0

[DefaultSpeciesSet]
compatibility_threshold = 3.0

[DefaultStagnation]
species_fitness_func = max
max_stagnation       = 20
species_elitism      = 2

[DefaultReproduction]
elitism            = 5
survival_threshold = 0.2
min_species_size = 1

